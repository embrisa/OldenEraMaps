# Example Fixture Coverage

The TypeScript tests read bundled `.rmg.json` examples from this directory.

These files are intentionally checked-in compatibility fixtures. Do not overwrite
them during ad hoc testing.
